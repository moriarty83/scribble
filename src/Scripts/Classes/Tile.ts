class Tile {
	tileNumber: number;
	isTop: boolean;
	isBottom: boolean;
	topPoints: number;
	bottomPoints: number;
	topMatches: Array<number>;
	bottomMatches: Array<number>;

	constructor(tileInfo: object) {}
}
